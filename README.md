# ansible-node-exporter
Ansible-based end-to-end monitoring setup using Prometheus, Node Exporter, and Grafana. This project automates deployment of a lightweight monitoring stack across Linux hosts. It installs Node Exporter on target machines, deploys Prometheus &amp; Grafana on a monitoring server (Docker-based), and auto-loads preconfigured dashboards.
